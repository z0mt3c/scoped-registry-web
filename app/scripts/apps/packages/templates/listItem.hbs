<h4 class="list-group-item-heading">
	<span class="badge pull-right">{{dist-tags.latest}}</span>
	{{name}}
</h4>

{{#if description}}
    <p class="list-group-item-text">asd{{description}}</p>
{{else}}
    <p class="list-group-item-text">No description</p>
{{/if}}
